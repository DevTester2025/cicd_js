const baseURL =
  "https://testapi-esko.cloudmatiq.com/cloudmatiq";
const webURL = "https://test-esko.cloudmatiq.com/";
const dashboardURL = "https://stats-test.cloudmatiq.com";
const dashboardAPIURL = "https://stats-testapi.cloudmatiq.com";
const orchURL ="https://testapi-esko.cloudmatiq.com/cloudmatiq";
export const environment = {
  production: true,
  baseURL: baseURL,
  weburl: webURL,
  dashboardURL,
  dashboardAPIURL,
  orchURL: orchURL

};

/*
 * In development mode, to ignore zone related error stack frames such as
 * `zone.run`, `zoneDelegate.invokeTask` for easier debugging, you can
 * import the following file, but please comment it out in production mode
 * because it will have performance impact when throw error
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
