        name: Rollback_Test2

        on:
          workflow_dispatch:


        jobs:
          build:
           name: GITHUB
           runs-on: ubuntu-latest
           steps:
                - name: Check out the repo
                  uses: actions/checkout@v3  
            
# No SonarQube configuration found for this project type.

    #---------------- BUILD_SCRIPT
          BUILD_SCRIPT:
            needs: [build]
            runs-on: ubuntu-latest
            steps:
               - name: Run script on the VM
                 run: |
                   sshpass -p "a7X2p9L4kR" ssh -p 222 -o StrictHostKeyChecking=no omnisecure@13.60.242.225 <<EOF
                              export AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
                              export AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
                              export AWS_REGION=eu-north-1
                              cd ~/projects/apps/sample_project
                              source ~/.nvm/nvm.sh
                              nvm use default
                              node -v
                              cd ~/projects/apps/sample_project
                              pwd
                              rm -rf node_modules package-lock.json dist
                              npm install
                              ng build
                              aws s3 cp /home/omnisecure/projects/apps/sample_project/dist/angular-conduit/ s3://cm-static--dev/build_test/ --recursive --region eu-north-1
                   EOF
