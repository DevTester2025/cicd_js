        name: Rollback_Test2

        on:
          workflow_dispatch:


        jobs:
          build:
           name: GITHUB
           runs-on: ubuntu-latest
           steps:
                - name: Check out the repo
                  uses: actions/checkout@v3  
            
# No SonarQube configuration found for this project type.

    #---------------- BUILD_SCRIPT
          BUILD_SCRIPT:
            needs: [build]
            runs-on: ubuntu-latest
            steps:
               - name: Run script on the VM
                 run: |
                   sshpass -p "a7X2p9L4kR" ssh -p 222 -o StrictHostKeyChecking=no omnisecure@51.20.249.232 <<EOF
                              cd ~/projects/apps/sample_project
                              source ~/.nvm/nvm.sh
                              nvm use default
                              node -v
                              cd ~/projects/apps/sample_project
                              pwd
                              rm -rf node_modules package-lock.json dist
                              npm install
                              ng build
                   EOF
                   - name: Print working directory on GitHub runner
                     run: |
                        echo "Current directory on GitHub runner:"
                        pwd
                        echo "Listing files for debug:"
                        ls -la    
               - name: Copy Build to AWS S3 for Rollback
                 uses: keithweaver/aws-s3-github-action@v1.0.0
                 with:
                   command: cp
                   source: /home/omnisecure/projects/apps/sample_project/dist/angular-conduit/ 
                   destination: s3://cm-static--dev/cicd-build/
                   aws_access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                   aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                   aws_region: eu-north-1
