        name: Rollback_Test2

        on:
          workflow_dispatch:


        jobs:
          build:
           name: GITHUB
           runs-on: ubuntu-latest
           steps:
                - name: Check out the repo
                  uses: actions/checkout@v3  
            
# No SonarQube configuration found for this project type.

    #---------------- BUILD_SCRIPT
          BUILD_SCRIPT:
            needs: [build]
            runs-on: ubuntu-latest
            steps:
               - name: Run script on the VM
                 run: |
                   sshpass -p "a7X2p9L4kR" ssh -p 222 -o StrictHostKeyChecking=no omnisecure@51.21.247.48 <<EOF
                              cd ~/projects/apps/sample_project
                              source ~/.nvm/nvm.sh
                              nvm use default
                              node -v
                              cd ~/projects/apps/sample_project
                              pwd
                              rm -rf node_modules package-lock.json dist
                              npm install
                              ng build        
                   EOF                   
               - name: Upload to FTP Server
                 env:
                  FTP_SERVER: 51.21.247.48
                  FTP_USERNAME: omnisecure
                  FTP_PASSWORD: a7X2p9L4kR
                  REMOTE_PATH: '/ftp/deploy'
                  LOCAL_FILE: 'build/app.zip'
                 run: |
                  chmod +x ftp_upload.sh
                  ./ftp_upload.sh "$FTP_SERVER" "$FTP_USERNAME" "$FTP_PASSWORD" "$REMOTE_PATH" "$LOCAL_FILE"

